{
  "name": "computer-setup",
  "version": "0.0.1",
  "scripts": {
    "setup": "npm run update-scripts && npm run update-zsh",
    "update-scripts": "cd scripts && npm run update",
    "update-zsh": "cd zshrc && npm run update"
  }
}